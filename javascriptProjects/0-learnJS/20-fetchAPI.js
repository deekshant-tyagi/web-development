// Api -> application program interface
// The fetch api provides an interface for fetching(sending/receiving) resources.
// It uses Request and Response objects.
// The fetch() method is used to fetch resource(Data).
// syntax -> let promise = fetch(url,[options]).

// AJAX and JSON
// AJAX -> is Asynchronous JS & XML(format).
// JSON is Javascript Object Orientation.
// json() method:returns a second promise that revolves with the result of parsing the response body text as JSON.(Input is JSON,output is JS object).

// Reqeust and Response
// HTTP Verbs[explore on mdn]
// Response Status code.
// Headers -> provide additional info or data. 